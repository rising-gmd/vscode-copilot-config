- Before generating code, load and follow any matching instruction file from `.github/instructions/` for the area being worked on (e.g. `angular-typescript.instructions.md`, `testing-angular.instructions.md`, `security.instructions.md`).
- Before generating code, load and follow any matching skill from `skills/` relevant to the current task (e.g. `skills/angular/forms`, `skills/angular/testing`).
- If an agent-specific instruction file exists for the current agent (e.g. inside `.github/agents/`), load it before starting.