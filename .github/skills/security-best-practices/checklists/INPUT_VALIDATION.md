# Input Validation Checklist

- Validate types and ranges server-side.
- Use model binding DTOs and disallow over-posting.
- Sanitize HTML input or use a safe renderer.
- Log and monitor validation failures with correlation IDs.
