- Component Checklist

- Use `standalone: true` where possible
- Use `changeDetection: OnPush`
- Template-only members should be `protected`
- Use `readonly` for immutable inputs
- Prefer `[class.]` bindings for single toggles
- Add unit tests and a11y checks
